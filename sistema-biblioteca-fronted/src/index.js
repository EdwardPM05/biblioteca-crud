// src/index.js
import React from 'react';
import ReactDOM from 'react-dom/client';
// No importamos './index.css' ya que lo eliminaste.
import App from './App';
// reportWebVitals y su llamada han sido eliminados.

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// Todo el código relacionado con reportWebVitals ha sido removido.